// Visualize interpolated normals after mapping [-1, 1] to [0, 1].
vertex {
    output.position = uniform.mvp * input.position;
    output.varying0 = vec4(normalize(input.normal), 1);
}

fragment {
    output.color = input.varying0 * 0.5 + vec4(0.5, 0.5, 0.5, 0.5);
}
